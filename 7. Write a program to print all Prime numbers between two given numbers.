#include<stdio.h>

int main()
{
    int a,b,i,n,Flag=0;
    printf("Enter two number:");
    scanf("%d %d",&a,&b);
    for(n=a;n<=b;n++)
    {
        Flag=0;
      for(i=2;i<=n/2;i++)
    {
       if(n%i==0)
            Flag=1;
    }
    if(Flag==0)
    printf("%d ",n);

    }
    return 0;
}
